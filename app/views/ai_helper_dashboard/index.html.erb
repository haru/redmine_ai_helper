<h2><%= l(:label_ai_helper) %></h2>

<%= render_tabs ai_helper_dashboard_tabs %>

<% # The markdown parser and project_health/master_detail scripts are already
   # loaded globally by ai_helper/shared/_html_header.html.erb
   # (view_layouts_base_html_head hook); including them again here would
   # double-declare their top-level bindings. %>
<% content_for :header_tags do %>
  <%= stylesheet_link_tag 'ai_helper_rtl', plugin: 'redmine_ai_helper' if l(:direction) == 'rtl' %>
<% end %>
